import 'package:flutter/material.dart';

class AddStocksGreenButton extends StatelessWidget {
  const AddStocksGreenButton({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
